<appSettings>
  <add key="Setting1" value=""/>
  <add key="Setting2" value=""/>

  <!-- Turn on log4net internal debugging. Can help diagnose problems with ADONetAppenders,
       for example, but in order to use it effectively you will need some sort of
       "can't fail" default appender such as a console or debug appender. -->
  <add key="log4net.Internal.Debug" value="false"/>
</appSettings>
