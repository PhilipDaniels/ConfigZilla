param($installPath, $toolsPath, $package, $project)

Write-Host "ConfigZilla Init.ps1 (solution level initialisation) is running."

Import-Module (Join-Path $toolsPath "ConfigZilla.psm1")


function Main
{
}

Main
